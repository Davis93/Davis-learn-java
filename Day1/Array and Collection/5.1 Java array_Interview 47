Date： 2018/5/14
面试题47： 如果理解数组在java作为一个类？
        数组本质上是一个类，并且保存了数据类型的信息，通过成员变量形式保存数据,[],通过下标来访问数组元素；
        处理基本数据类型时，数据保存是为变量的值，默认为0；处理引用类型时，保存的是数据的引用，默认为null.
        
面试题48： new Object[5]是否创建了5个对象？
        基本类型数组，new时，会为数组分配指定长度内存空间，初值为0；引用型数组，分配指定长度空间，但不是保存数据，而是保存引用，初始值为null.
          new只是实例化了对象，将创建好的引用赋给 引用变量 eg: Object[] objArr=new Object[3]; objArr为引用变量啦
          若要为这些元素分配具体对象，需要分别指定。或者用{}来一起指定
         new Object[5]创建了一个数组实例，长度为5，每个元素均为null,未创建5个Object 对象；若要创建5个对象，需要为每个数组元素分别指定。
